{% extends 'base.html.twig' %}

{% block content %}
    <h1>Manage Training: {{ training.name }}</h1>
    <p>{{ training.description }}</p>

    <h2>Modules</h2>
    <ul>
        {% for module in modules %}
            <li>
                {{ module.name }}
                <a href="{{ path('delete_module', { 'trainingId': training.id, 'moduleId': module.id }) }}">
                   ❌
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}